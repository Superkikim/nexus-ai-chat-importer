export interface CatalogEntry {
    conversation_id: string;
    create_time: string;
    update_time: string;
    provider: string;
    notePath: string;
    messageCount: number;
    fileHash: string;
}
